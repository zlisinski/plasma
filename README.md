This is my test of the plasma graphics effect.

This code requires the SDL 1.2 library. It compiles on both Windows and Linux with the required libraries.

The createPaletteImages file has only been compiled and tested on Linux and requires the gd library.
